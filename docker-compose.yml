services:
  db:
    image: postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: synergy_db
    ports:
      - '5432:5432'
    volumes:
      - pg-data:/var/lib/postgresql

volumes:
  pg-data:
